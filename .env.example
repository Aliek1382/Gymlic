# Base URL of the Gymlic PHP API (backend-php).
# Baked into the bundle at build time, not read at runtime: after changing it
# you must rebuild and re-upload.
#   local:      http://127.0.0.1:8099
#   production: https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://127.0.0.1:8099
